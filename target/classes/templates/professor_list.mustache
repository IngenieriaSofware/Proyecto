<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Lista de Profesores</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen p-4">
<div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-4xl">
    <h1 class="text-3xl font-bold text-center text-gray-800 mb-6">
        Lista de Profesores
    </h1>

    {{#professors}}
        <!-- Si hay profesores -->
        <table class="min-w-full border border-gray-200 rounded-lg overflow-hidden">
            <thead class="bg-blue-600 text-white">
            <tr>
                <th class="py-3 px-4 text-left">ID</th>
                <th class="py-3 px-4 text-left">Nombre</th>
                <th class="py-3 px-4 text-left">Email</th>
                <th class="py-3 px-4 text-left">Departamento</th>
                <th class="py-3 px-4 text-left">Teléfono</th>
            </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
            {{#professors}}
                <tr>
                    <td class="py-3 px-4 text-gray-700">{{id}}</td>
                    <td class="py-3 px-4 text-gray-700">{{name}}</td>
                    <td class="py-3 px-4 text-gray-700">{{email}}</td>
                    <td class="py-3 px-4 text-gray-700">{{department}}</td>
                    <td class="py-3 px-4 text-gray-700">{{phone}}</td>
                </tr>
            {{/professors}}
            </tbody>
        </table>
    {{/professors}}

    {{^professors}}
        <!-- Si NO hay profesores -->
        <p class="text-center text-gray-600 text-lg">
            No hay profesores registrados todavía.
        </p>
    {{/professors}}

    <div class="flex justify-between mt-8">
        <a
                href="/dashboard"
                class="bg-gray-500 text-white py-2 px-4 rounded-md hover:bg-gray-600 transition duration-300"
        >
            Volver al Panel
        </a>
        <a
                href="/professor/new"
                class="bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 transition duration-300"
        >
            Agregar Profesor
        </a>
    </div>
</div>
</body>
</html>
